var Q = require('q');
var model = require('../models');

/**
 * 获取特定活动的记录列表
 */
module.exports.listByActivityId = function(activityId, offset, size) {};

/**
 * 批量添加游戏记录
 * 先写内存，再批量写数据库
 */
module.exports.add = function(addedRecord) {};

